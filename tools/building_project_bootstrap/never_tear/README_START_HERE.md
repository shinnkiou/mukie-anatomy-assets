# NEVER TEAR Building Factory — START HERE

Canonical brand spelling: **NEVER TEAR**.

This directory is the machine/public-safe bootstrap for the independent building-production project. It is separated from BP3D human auto-segmentation.

## Startup order
1. Read `PROJECT_MASTER_PROMPT.md`.
2. Read `REFERENCES.md` and `project_config.json`.
3. Load `task_queue.json` using `task_queue_schema.json`.
4. Read `BUILD_PIPELINE_AND_QA.md`.
5. Add user-provided building goals with stable task IDs while preserving original wording.
6. Create the fresh ChatGPT Automation only after cadence/time is supplied; use `AUTOMATION_BOOTSTRAP.md`.

## Core rule
Research → design → implement → QA → fix → save → read back. Structure geometry first; surface/textures second. Do not mark COMPLETE before relevant QA and canonical readback pass.

## Canon roles
- Google Drive: human-readable canon
- GitHub: machine/public-safe canon
- Base44: realtime workbench, execution cache, checkpoints

New artifacts use NEVER TEAR. Historical immutable identifiers containing old voice-input naming remain unchanged only for audit/restoration.
