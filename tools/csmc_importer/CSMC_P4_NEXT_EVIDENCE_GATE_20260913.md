# CSMC P4 next-evidence gate — 2026-09-13

Status: **FAIL-CLOSED ROUTING CONTRACT / ZERO EVIDENCE ACQUISITION / ZERO RUNTIME AUTHORIZATION**

## Why this exists

The current durable public-safe corpus has reached several explicit evidence boundaries:

- Companion C C-045: bounded owner-edge search exhausted with 0/3 decisive boundary-local edges.
- C-039/C-040 plus mainline cross-constraint audit: six I2 preserve-signature bits remain genuinely unresolved; the current corpus leaves all 20 quota-compatible assignments admissible.
- counted-BE real-target fixed-role hit/miss remains untested because the exact authorized raw pair is not currently present in the execution environment.
- C-046/mainline family-compatibility analyses prune family-wide codec interpretations but do not produce a binding or semantic promotion.

Repeatedly recombining the same corpus is therefore lower-value and risks reopening closed searches. The router encodes the next legal static action only when genuinely new admissible evidence is already present.

## Router

`csmc_p4_next_evidence_gate.py` accepts metadata flags only. It does not read model bytes, acquire files, touch MODELER, dispatch runtime work, or change Worker/STABLE gates.

### Accepted new-evidence routes

1. **Complete I2 discriminator artifact**
   - exactly records `2,3,8,14,15,21`;
   - six q24 equality bits (or already-normalized equivalent);
   - provenance must already be validated;
   - action: complete structural preserve signatures only; no semantic promotion.

2. **New decisive boundary-local owner edge**
   - one or more of `EXPLICIT_PARENT_RECORD_BOUNDARY`, `EXPLICIT_PARENT_LENGTH_FIELD`, `EXPLICIT_CONSUMER_CROSSREF`;
   - action: review that new edge only; do not rerun the six-surface C-045 search.

3. **Validated C-040 I1-I4 static unlock artifact**
   - validation is external to this router;
   - action: process only the accepted artifact class; intake acceptance is not semantic confirmation.

4. **Exact authorized raw `.clip` + `.csmc` pair reappears**
   - hashes must exactly match the established authorized identities;
   - action: run only the preregistered counted-BE fixed-role probe on `record_whole`, `stable_prefix_0_20`, `control_zone_21_27`, `preserved_island_25_26`;
   - entropy ceilings remain fail-closed cross-checks;
   - no post-hoc window widening after a miss.

## Current-state output

With none of those new inputs present, the router returns:

`NO_ADMISSIBLE_ACTION_CURRENT_DURABLE_CORPUS`

and zero selected static actions.

Even if an input flag says runtime is authorized, this router never sets runtime dispatch true. Runtime authorization remains a separate gate and cannot be recreated indirectly by static exhaustion.

## Closed actions mechanically recorded

- V4.2 idle-memory scan;
- broad GUID scanning;
- unchanged exact-qword LCS/literal-size rescans;
- duplicate `BND_197_TO_195` owner-edge search over the current durable corpus;
- guessing I2 signatures from family marginals or supergroup aesthetics;
- post-hoc counted-BE window expansion after a miss.

## Claim boundary

The gate is workflow control, not a parser breakthrough. Semantic owner/function, vertex/index/UV/material/bone/weight meanings, and real Blender emission remain unresolved/unproven. Production Worker, STABLE, Canary, and Control Gate states are unchanged.
