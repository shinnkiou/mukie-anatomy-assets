# Companion C — Run C-027

## QUESTION
Has the fixed Companion C snapshot reached a genuine static-analysis ceiling, and can that ceiling be stated as missing evidence edges rather than a generic request for more data?

## HYPOTHESIS
The current bottleneck is an evidence-input ceiling, not a lack of parser architecture or analysis methods.

## PROBE
Compare the available method stack against the slot blocker matrix and binding graph.

Available methods already include:
- semantics-free IR
- owner/container route map
- binding evidence contract
- preregistered counted-numeric scan
- false-positive budget
- binding-scope classifier
- static-intake validator
- end-to-end pipeline gate classifier

## REAL AGGREGATE RESULT
The current ceiling is `INPUT_EVIDENCE_CEILING`, not `METHOD_CEILING`.

Six explicit missing edges remain:
1. current +965 record regime/family -> confirmed codec
2. current bound region -> geometry semantics
3. current bound region -> index semantics
4. current model representation -> transform semantics
5. current model representation -> hierarchy semantics
6. current material-bearing region -> material semantics

Transform and hierarchy have useful supporting evidence from other surfaces, but current-model binding edges are absent. Geometry/index/material have no direct current semantic evidence in the fixed inventory.

No runtime request is required by Companion C itself. If pre-existing authorized isolated evidence later becomes available, the side lane already has intake and analysis contracts ready to consume it without mainline contact.

## NEW INFORMATION
- The current stopping point is an input-evidence ceiling, not a parser-design ceiling.
- Missing evidence can be named as six specific graph edges.
- The nearest actionable edge remains `E1_PLUS965_TO_CONFIRMED_CODEC`; C-018 is already preregistered to test it if isolated static input appears.

## CLOSED HYPOTHESES
- More parser architecture must be invented before any current-data binding can be tested: REJECTED.
- Companion C must request runtime/MODELER work to make future progress: REJECTED.
- “Need more data” is the most precise available diagnosis: REJECTED; the missing edges are now explicit.

## CONFIDENCE CHANGES
- input-evidence ceiling classification: HIGH -> VERY HIGH
- method readiness for the nearest codec-binding test: HIGH -> VERY HIGH
- current semantic slots: unchanged / unresolved

## NEXT QUESTION
Can all confirmed facts, rejected hypotheses, candidate grammar, record/container map, parser code, blocker matrix, and future high-information evidence contracts be assembled into one analysis-package manifest for clean optional handoff to the main line without merging anything?
