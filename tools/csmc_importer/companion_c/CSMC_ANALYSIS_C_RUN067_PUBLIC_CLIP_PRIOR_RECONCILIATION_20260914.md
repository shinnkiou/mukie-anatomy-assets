# CSMC ANALYSIS COMPANION C — C-067 public `.clip` architecture-prior reconciliation — 2026-09-14

Status: `COMPLETED`

## Why C-067 is admissible

C-066 stopped further numbered runs on infrastructure alone. After that stop, a genuinely new independent public-safe evidence axis arrived as Supabase row 184: `CSMC_PUBLIC_CLIP_3D_ARCHITECTURE_PRIOR_20260914`.

This is not another intake/checkpoint restatement. It adds a multi-source public architecture prior for the `.clip` ecosystem. Therefore it meets the prior resume rule for an independent public-safe constraint, while remaining non-proof for `.csmc`.

## New external evidence reconciled

The external static lane pinned and validated three public projects:

- `Aodaruma/clipfile-rs` @ `bd88467fa80e63ad48c6c713fbfb5a8a116d798a` (MIT)
- `youichi-uda/clip-clai` @ `76edfbf393868687107c2cdfdb750a70ca9ba7fe` (MIT)
- `LavenderSnek/clipdecode` @ `e5347a65202bc399bdd730d13187bc32aabdd4fa` (LGPL-2.1)

The strongest public structural prior is that `.clip` external payload references can use an external-ID / offset indirection layer involving `ExternalTableAndColumnName`, `ExternalChunk`, and `CHNKExta`-style external data. The public 3D-related names include `Canvas3DModelBank.BankData`, `Canvas3DModelLoader.ModelData`, `ModelData3D.Layer3DModelData`, `Manager3DOd.SceneData`, and `Canvas.Canvas3DModelDataLoaderIndex`.

For `clipfile-rs`, the external source record reports 5,889 checked `ExternalChunk` rows and zero physical offset mismatches.

External classification: `STRONG_PUBLIC_ARCHITECTURE_PRIOR_NOT_CSMC_PROOF`.

## Non-transfer boundary

The controlled CSMC fixtures use a SQLite `character` row with a `CLIP_STUDIO_3D_DATA2` BLOB envelope. That is not the same container architecture as the public `.clip` evidence.

C-067 therefore does **not** assert that `.csmc` contains `ExternalChunk`, `ExternalTableAndColumnName`, `Canvas3DModelLoader`, or any other public `.clip` table/column. It does not map the 2456-byte cadence, F02 prefix, geometry, indices, materials, UVs, bones, or weights.

Agreement across public projects is corroboration, not guaranteed statistically independent replication; community projects may share earlier references.

## Blind-lane contamination guard

The existing F02 direct-read/data-flow preregistration was created without these public names. It remains sealed and unchanged.

C-067 freezes the following rule:

1. public names may **not** guide the blind discovery pass;
2. public names may **not** score or rescue a blind result;
3. the blind result must first be independently sealed as direct-data-flow output;
4. only after sealing may name/path overlap be reported as non-proof corroboration;
5. no overlap still means `DO_NOT_FORCE` rather than steering the result back toward the public architecture;
6. proof-grade claims still require the existing static evidence validator-v2.

Current state has no sealed blind result, so the prior is classified:

`PUBLIC_PRIOR_ADMITTED_QUARANTINED_PENDING_BLIND_RESULT`

## Companion implementation

Added a fail-closed adapter that binds the exact three source pins/licenses and rejects:

- changing `.clip` evidence into direct `.csmc` evidence;
- allowing the prior to guide blind preregistration;
- allowing it to close the static evidence gate;
- source-pin/license swaps;
- wrong observed/target format;
- post-blind use without an independently sealed direct-data-flow result;
- a blind result that declares public-prior use during discovery.

Synthetic coverage also verifies that post-blind name overlap stays `NON_PROOF_CORROBORATION` and that a nonmatch cannot be forced into a match.

## Result

C-067 adds one architecture constraint axis for **post-blind comparison only**. It does not close a consumer edge and does not advance semantic/import readiness.

- `EXPLICIT_SERIALIZER_FIELD_READ`: UNRESOLVED
- `CONTROLLED_FIXTURE_TO_CONSUMER_MATCH`: UNRESOLVED
- geometry: UNRESOLVED
- index topology: UNRESOLVED
- physical F02 oracle: 0/30 / PENDING_MANUAL_ORACLE
- pipeline: STRUCTURAL_ONLY
- semantic promotion: 0
- Blender emit: BLOCKED
- runtime / MODELER / Worker / Canary / Control Gate: 0 from Companion C
- RIO-26 mutation: 0
- mainline mutation: 0
- automatic integration: false
- raw/private publication: false

## Resume

Highest-value next evidence remains either:

- an independently sealed blind direct-read/data-flow result that can then be compared against this public prior;
- validated direct F02 physical oracle observations;
- actual C-051 source/control evidence through the existing C-052 -> C-053 -> C-054 chain; or
- another independent public-safe constraint that changes a live hypothesis.
