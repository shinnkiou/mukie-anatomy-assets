# Companion C — Run C-032

## QUESTION
Has the fixed snapshot become exhausted across every non-duplicative static-analysis axis, such that further information gain requires one of the already-defined new static evidence inputs rather than reinterpretation of the same aggregates?

## PROBE
Audit the fixed Companion C snapshot by analysis axis while enforcing all duplicate-experiment prohibitions.

## REAL AGGREGATE RESULT
Snapshot state: `EXHAUSTED_FOR_NON_DUPLICATIVE_STATIC_ANALYSIS`.

Axis status:
- boundary grammar: exhausted at current aggregates
- record grammar: exhausted at current aggregates
- owner/container: exhausted at current aggregates
- numeric codec: exhausted at current aggregates
- semantic assignment: exhausted at current aggregates
- historical P3 evidence: exhausted at current aggregates
- parser architecture: READY

The current importer pipeline remains `STRUCTURAL_ONLY`.

The remaining unlock inputs are explicit and static-only:
1. `I1_ISOLATED_PLUS965_STATIC_REF`: pre-existing authorized isolated current +965 input, enabling unchanged C-018 preregistered codec-binding scan.
2. `I2_PUBLIC_SAFE_POSITIONAL_AGGREGATE`: ordered `record_index/supergroup_index/slot_index/length_blocks/preserve_signature` metadata, enabling C-029 slot-family correlation without raw bytes.
3. `I3_INTERPRETABLE_CONTROLLED_PAIR_OR_LABELED_DIFFERENTIAL`: already-existing interpretable differential evidence for semantic correlation.
4. `I4_CURRENT_MODEL_NODE_MAPPING_ARTIFACT`: already-existing mapping evidence connecting current representation to hierarchy/transform candidates.

None requires Companion C to operate runtime, MODELER, Worker, Canary, Control Gate, or mainline.

## NEW INFORMATION
- The fixed snapshot is genuinely exhausted under the no-duplicate rule.
- The stopping point is not “analysis ran out of ideas”; every remaining high-value question has a named missing input class.
- Parser architecture is ready to accept future evidence while preserving semantic and Blender-emission gates.

## CLOSED HYPOTHESES
- Another reinterpretation of the same boundary windows can materially advance the importer: REJECTED.
- Another repetition of +965 cadence/family marginals can identify owner/semantics: REJECTED.
- Numeric plausibility alone can bridge the missing current-model binding edge: REJECTED.
- Companion C must violate lane isolation to make future progress: REJECTED.

## CONFIDENCE CHANGES
- fixed-snapshot exhaustion classification: HIGH -> VERY HIGH
- parser/evidence-contract readiness: VERY HIGH
- current stage `STRUCTURAL_ONLY`: VERY HIGH
- geometry/index/transform/material/hierarchy semantics: unchanged / unresolved

## NEXT QUESTION
None within the current fixed snapshot without violating duplicate-analysis rules. Resume only when one of I1-I4 static input classes becomes available, or when the user explicitly asks for optional handoff/integration packaging. Do not auto-merge.
