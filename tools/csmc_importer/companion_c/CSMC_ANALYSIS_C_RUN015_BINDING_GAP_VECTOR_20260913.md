# CSMC ANALYSIS COMPANION C — RUN C-015 — BindingEvidence gap vector

Status: STATIC SIDE-LANE PRIORITIZATION PASS / ZERO MODELER / ZERO RUNTIME

## QUESTION
When existing candidates are evaluated against the C-014 BindingEvidence contract, which candidate is closest to a legitimate current-payload binding and exactly which fields are missing?

## PROBE
Evaluated three existing candidates only:
1. confirmed counted-BE codec -> bounded +965 character record/regime;
2. quaternion-like rotation candidate -> current node transform;
3. latent historical node topology -> current hierarchy.

No new byte scan, cadence fit, or runtime action.

Codec binding requires five fields:
- current payload mapping;
- bounded region;
- independently confirmed codec;
- recurrent exact decode fit;
- same structural-role recurrence.

Semantic binding additionally requires controlled differential, known-input correlation, localized effect, and an explicit semantic slot.

## RESULT
### COUNTED_BE_TO_PLUS965
Codec contract: **3/5 satisfied**, 2 missing:
- recurrent exact fit;
- same structural-role recurrence.

Semantic contract: 3/9 satisfied, 6 missing.

### QUATERNION_TO_CURRENT_NODE_TRANSFORM
Codec contract: **0/5 satisfied** for a current node payload binding. Missing current payload mapping, bounded node region, confirmed node codec, recurrent exact fit, and same-role recurrence.

Semantic contract: 1/9 satisfied only because `transform` is the candidate semantic slot; 8 missing.

### LATENT_NODE_TOPOLOGY_TO_CURRENT_HIERARCHY
Codec/current-layout contract: **0/5 satisfied**. ParamScheme supplies a schema hint but no current bounded hierarchy payload/codec binding.

Semantic contract: 1/9 satisfied only because `hierarchy` is the candidate slot; 8 missing.

Nearest current codec-binding candidate: **COUNTED_BE_TO_PLUS965**.

## INTERPRETATION
The highest-value next static question is no longer a broad search for matrices, quaternions, or node names. It is narrowly whether the already-bounded +965 character record corpus contains a recurrent exact instance of the independently confirmed counted-BE numeric grammar at the same structural role. If that evidence is absent from the side-lane stores, static analysis has reached a clean storage/input ceiling rather than an analytical ambiguity.

This prioritization does not assert that +965 actually uses the counted-BE codec; it only says that candidate requires the fewest missing contract fields.

## NEW INFORMATION
- Counted-BE -> +965 is quantitatively the nearest binding candidate: only 2/5 codec-contract fields missing.
- Quaternion -> current node transform is much farther from binding: 5/5 codec-contract fields missing.
- Historical node topology -> current hierarchy is likewise 5/5 short of a current-layout/codec binding.
- The next efficient static test, if an isolated authorized input exists, is specifically recurrent counted-BE exact-fit at stable +965 structural roles.

## CLOSED HYPOTHESES
- Quaternion/node-transform is currently as close to a binding as counted-BE/+965: REJECTED.
- Latent node topology is currently as close to a current-layout binding as counted-BE/+965: REJECTED.
- Broad semantic searches should take priority over a narrow +965 codec-binding test: REJECTED under the C-014 gap metric.

## CONFIDENCE CHANGES
- counted-BE/+965 as highest-value static binding candidate: UNKNOWN -> HIGH.
- quaternion/current-node as near-term static binding candidate: MEDIUM -> LOW.
- latent topology/current hierarchy as near-term static binding candidate: MEDIUM -> LOW.
- any real codec binding currently confirmed: remains NONE.

## NEXT QUESTION
Does the dedicated Companion C storage itself contain an isolated authorized artifact sufficient to test recurrent counted-BE exact fits inside the bounded +965 record corpus, without reading mainline canonical state or touching runtime/MODELER?

## Guardrails
- Candidate priority is not semantic confidence.
- No codec is bound to +965 by this run.
- No mainline/canonical repoll.
