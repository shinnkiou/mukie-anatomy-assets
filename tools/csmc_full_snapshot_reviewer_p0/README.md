# CSMC FULL SNAPSHOT REVIEWER P0

PowerShell is the orchestrator, not the parser.

```
Snapshot ZIPs (read-only)
  -> 7-Zip listing + selective extraction
  -> rg targeted search (if installed)
  -> Python evidence classifier / deduper
  -> small REPORTS directory
  -> ChatGPT
```

The P0 reviewer explicitly treats already-proven facts as **non-results**.

Primary target:

> Identify the first real consumer candidate that interprets the `character` DATA2 payload and constructs a MODELER internal object.

Secondary target:

> Find a plausible connection from that consumer/internal-object lane to the posed VBO upload lane.

No broad string/XREF rediscovery is performed. See `GUIDE_JA.txt`.
